{
  "name": "@ethql/erc20",
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rimraf dist",
    "build:ts": "npm run clean && patch-package && tsc && copyfiles -u 1 src/schema/**/*.graphql src/abi/** dist"
  },
  "dependencies": {
    "web3": "^1.0.0-beta.35",
    "@ethql/base": "^0.0.0"
  }
}
