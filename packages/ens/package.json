{
  "name": "@ethql/ens",
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rimraf dist",
    "build:ts": "npm run clean && patch-package && tsc -b"
  },
  "dependencies": {
    "@ethql/base": "^0.0.0",
    "lodash": "^4.17.10",
    "web3": "^1.0.0-beta.35",
    "graphql": "^0.13.2",
    "ez-ens": "^1.0.4"
  }
}
